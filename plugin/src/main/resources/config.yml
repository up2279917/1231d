# ByteCore Configuration

# Shop Settings
shops:
  allowed-containers:
    - BARREL
  enabled-by-default: BARREL
  protection-radius: 0
  display-items:
    enabled: true
    op-only: false
    float-height: 1.1
    float-amplitude: 0.05
    float-frequency: 1.0
    view-distance: 48
    update-frequency: 2

# Location Settings
locations:
  max-per-player: 3

# Server Information
server:
  name: "bytefi.sh"
  website: "https://mc.bytefi.sh"

# API Settings
api:
  port: 25578

# Server Rules
rules:
  - "Do not steal, destroy or attack people/pets unprovoked."
  - "Do not use any hacks or client modifications that would give you an unfair advantage."
  - "No vulgar or obscene language in the chat."
